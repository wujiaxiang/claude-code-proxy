[project]
name = "anthropic-proxy"
version = "0.1.0"
description = "Multi-backend proxy: Anthropic API entry with OpenAI-compatible gateway ports (targets.json driven)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi[standard]>=0.115.11",
    "uvicorn>=0.34.0",
    "httpx>=0.25.0",
    "pydantic>=2.0.0",
    "python-dotenv>=1.0.0",
    "tiktoken>=0.7.0",
    "cryptography>=42.0.0",
]

